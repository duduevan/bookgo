---
interface Props {
  items: Array<{ name: string; path: string }>;
}

const { items } = Astro.props;
const last = items.length - 1;
---

<nav class="crumbs" aria-label="Você está aqui">
  <ol>
    {
      items.map((item, index) => (
        <li>
          {index === last ? (
            <span aria-current="page">{item.name}</span>
          ) : (
            <a href={item.path}>{item.name}</a>
          )}
        </li>
      ))
    }
  </ol>
</nav>

<style>
  .crumbs ol {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--s-2);
    list-style: none;
    padding: 0;
    margin: 0;
    font-size: var(--t--1);
    color: var(--color-muted);
  }

  .crumbs li + li::before {
    content: '/';
    margin-right: var(--s-2);
    color: var(--color-border);
  }

  .crumbs a {
    color: var(--color-muted);
    text-decoration: none;
  }

  .crumbs a:hover {
    color: var(--color-primary);
    text-decoration: underline;
  }

  /* Mobile: o último item é o título da página, que o H1 logo abaixo já
     diz. Esconder só no visual — o JSON-LD segue com a trilha inteira. */
  @media (max-width: 46rem) {
    .crumbs li:last-child:not(:only-child) {
      display: none;
    }
  }
</style>
